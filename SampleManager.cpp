#include "SampleManager.h"

// This file should remain empty since all methods are already defined 
// inline in the SampleManager.h header file
// The original implementation caused "redefinition" errors because
// the methods were defined both in the header and here